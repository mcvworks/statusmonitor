import { MyStackPage } from "./MyStackPage";

export const metadata = {
  title: "My Stack — DTMonitor",
  description: "Define your infrastructure stack for personalized blast radius analysis",
};

export default function MyStackRoute() {
  return <MyStackPage />;
}
